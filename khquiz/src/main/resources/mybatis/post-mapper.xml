<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.post">

	<!-- 등록 -->
	<select id="sequence" resultType="int">
		select post_seq.nextval from dual
	</select>
	<insert id="add">
		insert into post(
			post_no, post_title,post_content,
			post_password
		)
		values(
			#{postNo},#{postTitle},#{postContent},
			#{postPassword}
		)
	</insert>
	
	<!-- 목록 -->
	<select id="list" resultType="PostDto">
		select * from post order by post_no asc
	</select>
	
	<!-- 상세 -->
	<select id="find" resultType="PostDto">
		select * from post where post_no = #{postNo}
	</select>
	
	<delete id="delete">
		delete post where post_no = #{postNo}
	</delete>
	
	<update id="update">
		update 	post
		set		post_title = #{postTitle},
				post_content = #{postContent},
				post_utime =#{postUtime}
		where post_no = #{postNo}
	</update>
	
	
</mapper>